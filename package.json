{
	"name": "copy-for-slack",
	"version": "0.1.0",
	"description": "Obsidian plugin to copy selected text formatted for Slack",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"deploy": "npm run build && cp main.js manifest.json styles.css ~/private_vault/.obsidian/plugins/copy-for-slack/",
		"test": "vitest run"
	},
	"keywords": [],
	"author": "silentc",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"builtin-modules": "3.3.0",
		"esbuild": "0.17.3",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4",
		"vitest": "^4.1.0"
	}
}
